#include <cstdio>

int main()
{
    // 模拟输入
    int n = 5; // 鸡蛋数量
    int eggs[] = {100, 200, 300, 400, 500}; // 每个鸡蛋增加的分数

    // 初始化分数
    int score = 0;

    // 模拟处理每个鸡蛋
    for (int i = 0; i < n; ++i)
    {
        score += eggs[i]; // 累加分数
        if (score <= 500)
        {
            // 正常情况下这里会有输出，但现在我们去掉输出
            // printf("A");
        }
        else
        {
            score -= 1000;
            // 正常情况下这里会有输出，但现在我们去掉输出
            // printf("G");
        }
    }

    // 注意：此处没有输出，因为要求去掉输出部分
    // 正常情况下，这里可以添加一些代码来使用变量score，比如进行断言检查等

    return 0;
}