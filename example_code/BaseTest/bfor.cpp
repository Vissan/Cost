#include <iostream> // 仅在需要查看结果时包含

int main() {
    int numbers[5] = {1, 2, 3, 4, 5};

    // 使用for循环遍历数组
    for (int i = 0; i < 5; ++i) {
        // 这里可以进行一些操作，比如计算总和
        // ...（此处没有输出语句）
    }

    // 注意：这里没有使用cout或其他输出语句

    return 0;
}